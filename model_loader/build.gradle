archivesBaseName = "model_loader"
version = getSubprojectVersion(project)

moduleDependencies(project, [
		"client_events",
		"extensions",
		"accessors",
		"utility",
		"common"
])

//loom.accessWidenerPath = file("src/main/resources/porting_lib-model.accesswidener")

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
