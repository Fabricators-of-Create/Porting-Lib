portingLib {
	moduleDependencies.addAll("model_loader", "transfer")
	enableTestMod()
}

dependencies {
	modCompileOnly("io.vram:frex-fabric:$frex_version")
}

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
