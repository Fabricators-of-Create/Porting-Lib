moduleDependencies(project, [
		"core",
		"asm"
])

dependencies {
	modApi(include("com.jamieswhiteshirt:reach-entity-attributes:$rea_version"))
}

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
