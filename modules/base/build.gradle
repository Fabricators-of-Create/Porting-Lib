portingLib.addModuleDependencies([
		"lazy_registration",
		"extensions",
		"transfer",
		"utility",
		"entity",
		"common",
		"tags"
])

dependencies {
	modCompileOnly("io.vram:frex-fabric:$frex_version")
}
