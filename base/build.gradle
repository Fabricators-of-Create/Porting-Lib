moduleDependencies(project, [
		"lazy_registration",
		"fake_players",
		"extensions",
		"transfer",
		"utility",
		"common",
		"tags"
])

mixinExtras(dependencies)
fabricAsm(dependencies)

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
