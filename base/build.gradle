moduleDependencies(project, [
		"lazy_registration",
		"extensions",
		"transfer",
		"utility",
		"entity",
		"common",
		"tags"
])

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
