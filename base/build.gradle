archivesBaseName = "base"
version = "1.0"

moduleDependencies(project, [
		'model-loader',
		'extensions',
		'obj-loader',
		'transfer',
		'entity',
		'common',
		'models'
])

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
