{
  "schemaVersion": 1,
  "id": "porting_lib_recipe_categories",
  "version": "${version}",
  "name": "Porting Lib Recipe Category Api",
  "description": "Adds a api for adding custom recipe categories."
}
