archivesBaseName = "obj-loader"
version = getSubprojectVersion(project)

moduleDependencies(project, [
		"model-loader",
		"extensions",
])

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
