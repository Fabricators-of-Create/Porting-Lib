archivesBaseName = "accessors"
version = getSubprojectVersion(project)

loom {
	accessWidenerPath = file("src/main/resources/porting_lib.accesswidener")
}

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
