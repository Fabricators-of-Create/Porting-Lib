moduleDependencies(project, [
		"model_generators",
		"models"
])

loom.accessWidenerPath = file("src/main/resources/model-builders.accesswidener")

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
