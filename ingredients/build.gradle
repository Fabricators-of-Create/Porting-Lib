moduleDependencies(project, [
		"constants"
])

dependencies {
	modImplementation(include("io.github.tropheusj:serialization-hooks:${project.serialization_hooks_version}")) { transitive(false) }
}

processResources {
	duplicatesStrategy = DuplicatesStrategy.WARN
}
